import { TopMenuBar } from './TopMenuBar';

export function HUD() {
  return (
    <>
      <TopMenuBar />
      {/* Future: Side panels, bottom info panel, floating windows */}
    </>
  );
}
